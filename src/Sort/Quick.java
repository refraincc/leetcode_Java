package Sort;

public class Quick extends<sort> {
    /*
    1.从序列终选择一个轴点元素（pivot）
        假设每次选择0位置的元素为轴点元素
    2.利用轴点元素将序列分割成2个子序列
        将小于轴点的元素放在轴点的前边（左侧）
        将大于轴点的元素放在轴点的后边（右侧）
        等于轴点的元素放在任意一边都可以
    3.对子序列进行1，2操作
        直到不能再分割（子序列终只剩下1个元素）

    快速排序的本质
        逐渐将每一个元素都转换成轴点元素
    
    */
}
